#- name: install pip
#  apt:
#    name: python3-pip
#    state: present
#
- name: install python3-docker and docker-compose
  pip:
    name:
      - python3-docker
      - docker-compose
    state: present
#    executable: pip3
#    extra_args: --break-system-packages

- name: run container
  docker_container:
    name: wartest
    image: "{{ image }}"
    ports: 8081:8080
    state: started
 
